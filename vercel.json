{
  "framework": "nextjs",
  "installCommand": "npm config set registry https://registry.npmjs.org && npm ci --no-audit --no-fund",
  "buildCommand": "npm run build",
  "build": {
    "env": {
      "NODE_VERSION": "20"
    }
  }
}
